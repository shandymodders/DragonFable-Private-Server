DragonFable Multiplayer v2.4 Patch

Patch files only.

Target fixes:
- runtime appearance refresh faster
- reduce delay when weapon/cape/helm changes
- keep v2.3 native avatar system

Install by overwriting matching files and rebuilding the patched SWF.
